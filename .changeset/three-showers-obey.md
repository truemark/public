---
"truemark-cdk-lib": minor
---

Add AWS WorkSpaces constructs and supporting modules: `aws_workspaces`, `aws_patterns_workspaces`, `aws_directory_service` (Managed AD, Simple AD, directory groups), and SSM helpers `aws_ssm_activation` (hybrid activations) and `aws_ssm_patch` (patch baselines and run-shell-script associations).
