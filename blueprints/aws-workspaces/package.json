{
  "name": "aws-workspaces",
  "version": "0.1.0",
  "private": true,
  "description": "Deploys HIPAA-compliant AWS WorkSpaces with managed directory and encrypted storage.",
  "homepage": "https://github.com/truemark/public/tree/main/blueprints/aws-workspaces",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "cdk": "dist/cdk.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:truemark/public.git"
  },
  "scripts": {
    "prebuild": "biome check",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "vitest run",
    "clean": "rm -rf cdk.out dist coverage",
    "cdk": "cdk",
    "fix": "biome check --write .",
    "fmt": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "aws-cdk": "catalog:",
    "esbuild": "catalog:",
    "ts-node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "aws-cdk-lib": "catalog:",
    "constructs": "catalog:",
    "truemark-cdk-lib": "workspace:"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
